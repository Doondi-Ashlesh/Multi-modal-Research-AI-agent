# Copy to .env and fill in your keys

# OpenAI (or compatible API) - used for text + vision
OPENAI_API_KEY=sk-...

# Optional: base URL for compatible APIs (e.g. Azure, local)
# OPENAI_API_BASE=https://your-endpoint.com/v1

# Model for research (vision-capable for images/PDFs)
OPENAI_MODEL=gpt-4o
